{
  "name": "distributed-music-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-device synchronized audio playback",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm start",
    "install:all": "cd server && npm install && cd ../client && npm install"
  }
}
